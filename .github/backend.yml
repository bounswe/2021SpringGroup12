name: CD
on:
  push:
    branches: [ dev/backend ]
  pull_request:
    branches: [ dev/backend ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup JDK
        uses: actions/setup-java@v2.3.1
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: 'gradle'
      - name: Build image
        run: ./application/backend/gradlew bootBuildImage
      - name: Docker login
        uses: docker/login-action@v1.10.0
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Push image to the repo
        run: |
            docker tag beabee/beabee_spring:latest beabeeapp/beabee
            docker push beabeeapp/beabee
  deploy:
    runs-on: ubuntu-latest
    needs: publish
    
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

# TODO Waiting for AWS registaration to cmoplete.

