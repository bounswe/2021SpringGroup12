<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <TextView
            android:id="@+id/tv_descriptionTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:layout_marginEnd="15dp"
            android:text="Token:"
            android:textSize="20sp"
            app:layout_constraintStart_toStartOf="parent" />

        <EditText
            android:id="@+id/et_description"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:hint="Enter the Token"
            android:text=""
            android:textSize="20sp"
            android:ellipsize="end"
            android:maxLines="1"
            android:inputType="text"
            app:layout_constraintStart_toEndOf="@+id/tv_descriptionTitle"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/tv_descriptionTitle"
            app:layout_constraintBottom_toBottomOf="@id/tv_descriptionTitle"/>

        <Button
            android:id="@+id/btn_token"
            android:layout_width="100dp"
            android:layout_height="50dp"
            android:text="GO!"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="@id/et_description"
            app:layout_constraintStart_toStartOf="@id/tv_descriptionTitle"
            app:layout_constraintTop_toBottomOf="@id/tv_descriptionTitle" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView>